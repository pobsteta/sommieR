% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verifier.R
\name{sommier_verifier_chaine}
\alias{sommier_verifier_chaine}
\title{Verification d'une chaine d'entrees de sommier}
\usage{
sommier_verifier_chaine(entrees, foret_id = NULL, hash_prev_initial = NULL)
}
\arguments{
\item{entrees}{\code{data.frame} ou liste de listes nommees, portant au moins
les colonnes de \link{SOMMIER_CHAMPS_EMPREINTE} plus \code{hash} et \code{hash_prev}.
Les empreintes sont acceptees en \code{raw} ou en hexadecimal. Le \code{payload}
peut etre une liste R ou du texte JSON (recanonise via
\code{\link[=jcs_depuis_json]{jcs_depuis_json()}}).}

\item{foret_id}{Identifiant de la foret ; deduit des entrees si absent.}

\item{hash_prev_initial}{Empreinte precedant la premiere entree fournie.
Par defaut, la genese de la foret - a renseigner pour verifier un
fragment de chaine a partir d'une ancre.}
}
\value{
Un objet de classe \code{sommier_rapport} : liste comportant \code{valide}
(booleen), \code{n_entrees}, \code{foret_id}, \code{seq_tete}, \code{hash_tete} (hexadecimal)
et \code{anomalies} (\code{data.frame} a colonnes \code{seq}, \code{id}, \code{type}, \code{message}).
}
\description{
Recalcule la chaine de la genese a la tete et confronte chaque empreinte
stockee a l'empreinte recalculee. C'est la fonction que le brief
(section 6.3) designe comme utilisable "par un auditeur tiers sur un
export" : elle ne touche pas la base et ne fait confiance a aucun serveur,
elle ne travaille que sur les donnees qu'on lui remet.
}
\details{
Cinq classes d'anomalies sont distinguees, parce qu'elles ne se corrigent
pas de la meme facon :
\itemize{
\item \code{sequence_manquante} : un trou dans \code{seq} - une entree a ete retiree de
l'export, ou n'a jamais ete inseree.
\item \code{sequence_dupliquee} : deux entrees portant le meme \code{seq} - signe d'une
fourche d'ecriture concurrente que le verrou consultatif aurait du
empecher.
\item \code{chainage_rompu} : le \code{hash_prev} d'une entree ne correspond pas au
\code{hash} de la precedente - une entree a ete inseree, retiree ou reordonnee.
\item \code{empreinte_invalide} : le \code{hash} stocke ne correspond pas au recalcul sur
le contenu - le contenu de l'entree a ete modifie apres coup.
\item \code{genese_invalide} : la premiere entree ne part pas de
\code{\link[=sommier_empreinte_genese]{sommier_empreinte_genese()}} - la chaine a ete tronquee par le debut.
}

Une chaine dont la premiere entree n'a pas \code{seq == 1} est signalee comme
tronquee : la verification reste possible a partir d'une ancre connue, mais
l'appelant doit alors fournir \code{hash_prev_initial}.
}
\examples{
entrees <- sommier_chainer(list(
  sommier_entree(
    foret_id = "3f2b1c4d-5e6f-4a7b-8c9d-0e1f2a3b4c5d",
    registre = 5L, date_evenement = "2026-03-01", auteur = "agent-01",
    payload = registre5_coupe(
      type_entree = "martelage", exercice = 2026,
      nature_coupe = "amelioration", volume_m3 = 120
    )
  )
))
sommier_verifier_chaine(entrees)

}
