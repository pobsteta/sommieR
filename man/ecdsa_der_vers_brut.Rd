% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jws.R
\name{ecdsa_der_vers_brut}
\alias{ecdsa_der_vers_brut}
\title{Conversion d'une signature ECDSA du DER vers le format JOSE}
\usage{
ecdsa_der_vers_brut(der, taille = TAILLE_COMPOSANTE_ES256)
}
\arguments{
\item{der}{Signature encodee en DER (\code{raw}).}

\item{taille}{Taille d'une composante, en octets. 32 pour P-256.}
}
\value{
Un vecteur \code{raw} de \code{2 * taille} octets.
}
\description{
OpenSSL encode une signature ECDSA en DER (\verb{SEQUENCE \{ INTEGER r, INTEGER s \}}), JOSE l'attend en \code{R||S} : les deux composantes concatenees,
chacune sur la taille de la courbe.
}
\details{
Le rembourrage n'est pas une precaution de style. \code{openssl::ecdsa_parse()}
rend deux \code{bignum}, et un \code{bignum} ne porte pas ses zeros de tete : une
composante dont le premier octet est nul s'y presente sur 31 octets. Sur
4 000 signatures P-256 mesurees, 29 - soit 0,72 \% - ont au moins une
composante courte. Les concatener telles quelles produirait une signature de
63 octets, refusee par toute autre implementation JOSE, et le defaut ne se
manifesterait qu'une fois sur cent quarante.
}
\seealso{
\code{\link[=ecdsa_brut_vers_der]{ecdsa_brut_vers_der()}}
}
