% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cms.R
\name{tsa_verifier_jeton}
\alias{tsa_verifier_jeton}
\title{Verification d'un jeton d'horodatage}
\usage{
tsa_verifier_jeton(jeton, empreinte = NULL, ancres = list())
}
\arguments{
\item{jeton}{Vecteur \code{raw} : le \code{TimeStampToken}.}

\item{empreinte}{Empreinte attendue (\code{raw} de 32 octets), ou \code{NULL} pour ne
pas la confronter.}

\item{ancres}{Liste de certificats de confiance, lus par
\code{\link[=certificat_lire]{certificat_lire()}}. Aucune n'est embarquee dans le paquet : ce serait
faire dependre du rythme de publication de sommieR la question de savoir
qui est digne de confiance, et une racine retiree resterait attestee par
toute version installee.}
}
\value{
Un objet \code{sommier_verdict_tsa}.
}
\description{
Verifie qui atteste, et non plus seulement ce qui est atteste : la
signature de l'autorite sur le contenu du jeton, l'usage que son certificat
declare, sa validite a la date attestee, et la chaine qui le rattache a une
ancre de confiance.
}
\details{
\strong{Trois etats, pas deux.}
\itemize{
\item \code{"valide"} : tout est verifie, et la chaine remonte a une ancre fournie.
\item \code{"non_rattache"} : le jeton est intact - signature de l'autorite verifiee,
empreinte concordante, usages et dates bons - mais aucune ancre ne le
couvre, soit qu'aucune n'ait ete fournie, soit qu'aucune ne convienne.
\item \code{"invalide"} : quelque chose cloche dans le jeton lui-meme.
}

La distinction n'est pas cosmetique. Dire « invalide » a une commune dont le
jeton est parfait mais emis par une autorite qu'on n'a pas listee serait
faux, et lui faire croire a une garantie qu'on n'a pas verifiee le serait
tout autant.

\strong{Ce qui est verifie}, dans cet ordre :
\enumerate{
\item le contenu se lit, et l'empreinte attestee est celle attendue ;
\item le jeton porte un signataire et un seul (RFC 3161 section 2.4.2) ;
\item l'attribut \code{contentType} annonce bien un \code{TSTInfo} ;
\item l'attribut \code{messageDigest} correspond au condensat du contenu - sans
quoi la signature porterait sur autre chose que ce qu'on a lu ;
\item l'attribut \code{signingCertificate} designe le certificat employe, par son
empreinte : sans lui, un certificat substitue dans le champ
\code{certificates} passerait pour celui qui a signe ;
\item la signature porte sur les attributs signes, reencodes en \verb{SET OF}
(RFC 5652 section 5.4) ;
\item le certificat porte l'usage \code{id-kp-timeStamping} et lui seul ;
\item il etait valide \strong{a la date attestee}, non aujourd'hui ;
\item la chaine remonte a une ancre, chaque lien verifie a cette meme date.
}

\strong{Ce qui n'est pas verifie : la revocation.} CRL et OCSP demandent le
reseau, ce que la verification hors ligne exclut par construction. Un
certificat revoque mais non expire passe donc. La limite est reelle ; elle
est ecrite ici et rendue dans le verdict plutot que passee sous silence.
}
\seealso{
\code{\link[=tsa_lire_jeton]{tsa_lire_jeton()}}, \code{\link[=certificat_lire]{certificat_lire()}}
}
