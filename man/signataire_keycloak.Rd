% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jws.R
\name{signataire_keycloak}
\alias{signataire_keycloak}
\title{Signataire dont l'identite vient de Keycloak ou d'AgentConnect}
\usage{
signataire_keycloak(
  jeton_id,
  cle,
  kid = NULL,
  claims_retenus = c("sub", "given_name", "usual_name", "family_name", "email", "siret",
    "iss"),
  certificat = NULL
)
}
\arguments{
\item{jeton_id}{Jeton d'identite OIDC (JWT compact).}

\item{cle}{Cle privee servant a signer.}

\item{kid}{Identifiant de cle (facultatif).}

\item{claims_retenus}{Claims a archiver dans le visa. Par defaut ceux que
le brief nomme, plus \code{siret}.}

\item{certificat}{Certificat X.509 du signataire, en DER (\code{raw}).}
}
\value{
Un objet \code{sommier_signataire}.
}
\description{
Les claims sont extraits du jeton d'identite OIDC ; la signature reste
produite par la cle fournie.
}
\details{
Cette separation n'est pas un contournement, c'est le fonctionnement du
protocole : un fournisseur OIDC delivre des jetons attestant une identite,
il ne signe pas un contenu qu'on lui soumet. Le jeton prouve \strong{qui} est la
personne, la cle produit la signature detachee sur la tete de chaine. Pour
un niveau eIDAS qualifie, \code{cle} doit etre remplacee par un appel au service
de signature du prestataire - c'est exactement ce que permet le parametre
\code{signer} de \code{\link[=sommier_signataire]{sommier_signataire()}}.

Le jeton n'est pas verifie ici : sa validite releve du client OIDC qui l'a
obtenu. Ce qui est archive dans le visa, ce sont ses claims.
}
