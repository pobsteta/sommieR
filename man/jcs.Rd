% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jcs.R
\name{jcs}
\alias{jcs}
\title{Serialisation JSON canonique (RFC 8785, JCS)}
\usage{
jcs(x)
}
\arguments{
\item{x}{Valeur R a serialiser.}
}
\value{
Une chaine de caracteres de longueur 1, en UTF-8.
}
\description{
\code{jcs()} produit la representation canonique JCS d'une valeur R. C'est la
brique de base du chainage de hachages du sommier : deux representations
d'un meme payload doivent produire des octets strictement identiques, sinon
la chaine n'est pas verifiable par un tiers.

Le JSONB restitue par PostgreSQL ne convient pas (il reordonne les cles et
normalise les nombres) : conformement au brief, le hachage est calcule cote
R et la base ne fait que stocker et contraindre.
}
\details{
Regles appliquees (RFC 8785 sections 3.2.2 a 3.2.4) :
\itemize{
\item \strong{Objets} : cles triees par ordre des unites de code UTF-16, aucun
espace, \code{"cle":valeur}.
\item \strong{Tableaux} : ordre d'origine preserve.
\item \strong{Chaines} : echappement minimal RFC 8259 (\verb{\\"}, \verb{\\\\}, \verb{\\b}, \verb{\\f},
\verb{\\n}, \verb{\\r}, \verb{\\t}), autres caracteres de controle en \verb{\\u00xx} minuscule,
tout le reste litteral en UTF-8.
\item \strong{Nombres} : algorithme ECMAScript \code{Number::toString} (voir
\code{\link[=jcs_nombre]{jcs_nombre()}}). \code{NaN} et les infinis sont refuses, JSON ne les
representant pas.
\item \strong{Litteraux} : \code{true}, \code{false}, \code{null}.
}

Correspondance R -> JSON :\tabular{ll}{
   Valeur R \tab JSON \cr
   \code{NULL} \tab \code{null} \cr
   liste nommee \tab objet \cr
   liste non nommee \tab tableau \cr
   \code{structure(list(), names = character(0))} \tab \code{{}} \cr
   \code{list()} \tab \verb{[]} \cr
   atomique de longueur 1 \tab scalaire \cr
   atomique de longueur != 1 \tab tableau \cr
   atomique de longueur 1 dans \code{I()} \tab tableau a 1 element \cr
   \code{NA} \tab erreur (ambigu) \cr
}


Ces conventions sont celles de \code{jsonlite::toJSON(auto_unbox = TRUE)}, a
ceci pres que \code{NA} est refuse : \code{null} et "valeur manquante" ne sont pas
interchangeables dans un registre a valeur probante, il faut ecrire \code{NULL}
explicitement.
}
\examples{
jcs(list(b = 1, a = "x"))
#> {"a":"x","b":1}
jcs(list(volume_m3 = 12.5, essence = "HET"))

}
\seealso{
\code{\link[=jcs_nombre]{jcs_nombre()}}, \code{\link[=sommier_empreinte]{sommier_empreinte()}}
}
