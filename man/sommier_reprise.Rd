% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reprise.R
\name{sommier_reprise}
\alias{sommier_reprise}
\title{Construction d'une entree transcrite depuis l'existant}
\usage{
sommier_reprise(
  foret_id,
  registre,
  date_evenement,
  auteur,
  payload,
  source,
  ug_uuid = NULL,
  ndp = NULL,
  corrige_id = NULL,
  id = uuid_v4(),
  ...
)
}
\arguments{
\item{foret_id}{UUID de la foret.}

\item{registre}{Numero de registre, parmi \link{SOMMIER_REGISTRES_OUVERTS}.}

\item{date_evenement}{Date du fait transcrit (\code{Date} ou \code{"AAAA-MM-JJ"}).}

\item{auteur}{Identifiant de qui transcrit - pas de qui a constate a
l'epoque : c'est bien une ecriture d'aujourd'hui.}

\item{payload}{Liste nommee produite par un constructeur de registre.}

\item{source}{Bloc de provenance produit par \code{\link[=reprise_source]{reprise_source()}}.}

\item{ug_uuid}{UUID de l'unite de gestion, ou \code{NULL}.}

\item{ndp}{Niveau de precision (entier >= 1). Par defaut, celui que
\link{SOMMIER_SOURCES_REPRISE} attache a la provenance.}

\item{corrige_id}{UUID de l'entree que celle-ci rectifie, le cas echeant -
une transcription fautive se rectifie comme le reste, par une entree de
plus.}

\item{id}{UUID de l'entree ; genere si absent.}

\item{...}{Aucun autre argument n'est admis. \code{date_saisie} en particulier
est refuse, avec un message qui dit pourquoi.}
}
\value{
Un objet de classe \code{sommier_reprise}, qui herite de
\code{sommier_entree}.
}
\description{
Prepare une entree qui \strong{transcrit} un fait anterieur au sommier - une
coupe de 1998 lue dans le registre papier, un arrete retrouve aux archives
de la commune - au lieu de le constater. Comme \code{\link[=sommier_entree]{sommier_entree()}}, elle
rend une entree non encore chainee ; elle s'ecrit par \code{\link[=sommier_reprendre]{sommier_reprendre()}}.
}
\details{
Une reprise se distingue d'un constat sur trois points, et ces trois points
sont imposes ici plutot que laisses a la discipline de l'appelant :
\itemize{
\item \strong{\code{date_saisie} est l'instant reel de la transcription.} Le champ est
pose par la fonction et ne peut pas lui etre dicte : le lui permettre
laisserait antidater une reprise, et une chaine qui peut etre convaincue
d'avoir su plus tot qu'elle n'a su ne vaut rien. La date du fait, elle,
se porte par \code{date_evenement}, qui remonte aussi loin qu'il le faut.
\item \strong{Le NDP est strictement superieur a 0.} NDP 0 designe le constat de
terrain ; une recopie n'en est pas un. C'est la regle deja appliquee aux
detections par \code{\link[=sommier_importer_detections]{sommier_importer_detections()}}, transposee a une autre
provenance.
\item \strong{La source est citee.} Sans reference de piece, une reprise ne se
distingue pas d'une invention. Il n'y a donc pas de defaut a \code{source}.
}

La sequence, elle, n'est pas rejouee : les entrees reprises s'ajoutent a la
suite de ce qui est deja ecrit. Le sommier date l'histoire, il ne la
reordonne pas - une reprise de trente ans forme un bloc d'entrees contigues
dont les dates d'evenement remontent le temps.
}
\examples{
sommier_reprise(
  foret_id = "3f2b1c4d-5e6f-4a7b-8c9d-0e1f2a3b4c5d",
  registre = 5L,
  date_evenement = "1998-03-12",
  auteur = "agent-01",
  payload = registre5_coupe(
    type_entree = "martelage", exercice = 1998,
    nature_coupe = "amelioration", volume_m3 = 210
  ),
  source = reprise_source(
    "registre_signe",
    "Sommier papier, imprime A50E, exercice 1998, folio 12"
  )
)

}
\seealso{
\code{\link[=sommier_reprendre]{sommier_reprendre()}}, \code{\link[=reprise_source]{reprise_source()}}, \code{\link[=sommier_entree]{sommier_entree()}}
}
