% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visa.R
\name{sommier_verifier_visas}
\alias{sommier_verifier_visas}
\title{Verification des visas d'une foret}
\usage{
sommier_verifier_visas(con, foret_id, cles_publiques = list(), ancres = list())
}
\arguments{
\item{con}{Connexion DBI.}

\item{foret_id}{UUID de la foret.}

\item{cles_publiques}{Liste nommee de cles publiques, indexee par \code{kid} ou
par \code{sub} du signataire. Inutile pour les visas portant leur certificat.}

\item{ancres}{Ancres de confiance pour les jetons d'horodatage, lues par
\code{\link[=certificat_lire]{certificat_lire()}}. Sans elles, un jeton intact est dit
\code{"non_rattache"} plutot que valide.}
}
\value{
Un \code{data.frame} : \code{exercice}, \code{autorite}, \code{seq_tete}, \code{concorde},
\code{signature_valide}, \code{horodatage}, \code{date_attestee}, \code{remarque}.
}
\description{
Confronte chaque visa a la chaine : l'empreinte attestee correspond-elle
bien a l'entree de la sequence visee, et la signature est-elle valide sous
la cle fournie ?
}
\details{
Les cles publiques sont passees par l'appelant, indexees par \code{kid} ou par
\code{sub}. Le paquet ne va pas les chercher au JWKS du fournisseur : cela
ferait dependre une verification a valeur probante de la disponibilite d'un
service tiers au moment du controle, alors qu'un visa doit rester
verifiable des annees plus tard, hors ligne.

Un visa sans jeton d'horodatage est signale mais n'invalide rien : sa date
repose sur l'horloge du serveur, ce que l'appelant doit savoir sans que
cela constitue une fraude.

Depuis la v0.10.0, un visa peut porter le certificat de son signataire.
Quand il en porte un, la cle en est tiree et \code{cles_publiques} devient
inutile : le visa se verifie seul. Les visas anterieurs gardent le
comportement precedent.

\code{date_attestee} est lue \strong{dans le jeton}, non dans la base : c'est la date
que l'autorite a certifiee. La colonne \code{date_visa}, elle, est celle que le
registre s'est donnee a lui-meme, et ne prouve rien contre celui qui tient
la base.

\code{horodatage} porte quatre etats plutot qu'un booleen, parce qu'un jeton se
juge sur plus que sa presence : \code{"absent"}, \code{"valide"} (signature de
l'autorite verifiee et chaine rattachee a une ancre), \code{"non_rattache"} (le
jeton est intact, mais aucune ancre ne le couvre) et \code{"invalide"}.

La revocation des certificats n'est jamais verifiee : CRL et OCSP demandent
le reseau. Voir \code{\link[=tsa_verifier_jeton]{tsa_verifier_jeton()}}.
}
