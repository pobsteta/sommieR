% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sql.R
\name{decouper_sql}
\alias{decouper_sql}
\title{Decoupage d'un script SQL en instructions}
\usage{
decouper_sql(sql)
}
\arguments{
\item{sql}{Le script, en une chaine de caracteres.}
}
\value{
Un vecteur de caracteres : une instruction par element, sans le
point-virgule final, les instructions vides ecartees.
}
\description{
Separe un script sur les points-virgules de premier niveau. Necessaire
parce que les pilotes qui passent par le protocole etendu de PostgreSQL
(RPostgres, via une instruction preparee) refusent plusieurs commandes en
un seul envoi : « cannot insert multiple commands into a prepared
statement ». Les pilotes en protocole simple l'acceptent, ce qui rend le
defaut invisible tant qu'on ne change pas de pilote.
}
\details{
Un simple \code{strsplit(sql, ";")} ne convient pas : le schema du sommier
contient des corps de fonction plpgsql delimites par \verb{$$}, qui portent
leurs propres points-virgules. Le decoupage suit donc l'etat lexical du
script :
\itemize{
\item chaines \code{'...'}, ou \code{''} designe une apostrophe litterale et ne ferme
pas la chaine ;
\item identifiants entre guillemets \code{"..."} ;
\item blocs delimites par le dollar, \verb{$$...$$} ou \verb{$tag$...$tag$}, ou seul le
meme tag ferme le bloc ;
\item commentaires de ligne \code{-- ...} ;
\item commentaires de bloc, imbricables en PostgreSQL.
}
}
\examples{
decouper_sql("SELECT 1; SELECT 2;")

}
