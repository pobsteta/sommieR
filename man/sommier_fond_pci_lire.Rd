% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pci.R
\name{sommier_fond_pci_lire}
\alias{sommier_fond_pci_lire}
\title{Lecture d'une couche du PCI vecteur}
\usage{
sommier_fond_pci_lire(
  fond,
  couche = "bornes",
  emprise = NULL,
  marge_m = 100,
  symboles = NULL
)
}
\arguments{
\item{fond}{Objet \code{sommier_fond_pci}.}

\item{couche}{L'une des noms de \link{SOMMIER_COUCHES_PCI}.}

\item{emprise}{Couche des unites de gestion, ou \code{data.frame} a colonne
\code{wkt} en Lambert-93.}

\item{marge_m}{Marge autour de l'emprise, en metres.}

\item{symboles}{Vecteur nomme donnant la nature de chaque code \code{SYM}, par
exemple \code{c("21" = "mur", "22" = "fosse")}. Il vous appartient : le paquet
n'en embarque aucun tant qu'une source n'est pas citable.}
}
\value{
Un \code{data.frame} : \code{feuille}, \code{objet}, \code{sym}, \code{nature}, \code{wkt}.
}
\description{
Lit une couche EDIGEO sur les feuilles telechargees, restreinte a l'emprise
de la foret.
}
\details{
\strong{La nature d'un detail n'est pas devinee.} \code{TLINE_id} porte un attribut
\code{SYM} qui distingue mur, fosse, haie et cloture, mais sa nomenclature ne
figure ni dans le \code{.DIC} ni dans le \code{.SCD} de l'archive : elle appartient a
la symbolisation du plan, publiee ailleurs. Le code est donc rendu tel quel,
et \code{symboles} permet de fournir la correspondance. Sans elle, \code{nature} reste
\code{NA} - un paquet dont l'objet est la valeur probante ne peut pas afficher
« fosse » la ou le terrain montre un mur.

Le systeme de coordonnees vient de la \strong{declaration du lot}. EDIGEO est
auto-descripteur : le fichier \code{.GEO} porte le referentiel employe
(\code{LAMB93} pour la metropole, \code{CC42} a \code{CC50} pour les livraisons
\code{edigeo-cc}). Le pilote de GDAL, lui, rend un proj4 sans code EPSG. On lit
donc la declaration a la source, et la sortie est ramenee en Lambert-93 quel
que soit le lot. Un referentiel non reconnu est signale plutot que
reinterprete - reprojeter au hasard poserait la feuille a cote de la foret.
}
\examples{
# Necessite `sf` et un fond telecharge :
# sommier_fond_pci_lire(fond, "bornes")

}
