% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/entree.R
\name{sommier_entree}
\alias{sommier_entree}
\title{Construction d'une entree de sommier}
\usage{
sommier_entree(
  foret_id,
  registre,
  date_evenement,
  auteur,
  payload,
  ug_uuid = NULL,
  ndp = 0L,
  corrige_id = NULL,
  date_saisie = Sys.time(),
  id = uuid_v4()
)
}
\arguments{
\item{foret_id}{UUID de la foret.}

\item{registre}{Numero de registre, parmi \link{SOMMIER_REGISTRES_OUVERTS}.}

\item{date_evenement}{Date de l'evenement enregistre (\code{Date} ou
\code{"AAAA-MM-JJ"}) - celle du fait, pas celle de la saisie.}

\item{auteur}{Identifiant de l'auteur (\code{sub} OIDC Keycloak).}

\item{payload}{Liste nommee produite par \code{\link[=registre5_coupe]{registre5_coupe()}} ou
\code{\link[=registre6_travaux]{registre6_travaux()}}.}

\item{ug_uuid}{UUID de l'unite de gestion, ou \code{NULL} pour une entree a
l'echelle de la foret.}

\item{ndp}{Niveau de precision nemeton (entier >= 0, defaut 0).}

\item{corrige_id}{UUID de l'entree que celle-ci rectifie, le cas echeant.}

\item{date_saisie}{Instant de saisie (\code{POSIXct} ou \code{"AAAA-MM-JJTHH:MM:SSZ"}).
Par defaut l'instant courant en UTC. Il est couvert par l'empreinte, il
doit donc etre fixe par l'ecrivain, pas par l'horloge du serveur.}

\item{id}{UUID de l'entree ; genere si absent.}
}
\value{
Un objet de classe \code{sommier_entree}.
}
\description{
Prepare une entree valide mais \strong{non encore chainee} : \code{seq}, \code{hash_prev}
et \code{hash} sont affectes au moment de l'insertion, par \code{\link[=sommier_chainer]{sommier_chainer()}}
hors base ou par \code{\link[=sommier_ajouter]{sommier_ajouter()}} en base. Cette separation est
volontaire - la position dans la chaine n'est pas une propriete de
l'entree, c'est une propriete du registre au moment ou on l'y ecrit.
}
\details{
Le champ \code{ndp} raccorde le sommier au systeme nemeton (brief, section 4) :
une entree saisie sur le terrain est NDP 0 par definition, une entree
deduite de teledetection porte le NDP de sa source. La valeur par defaut
est donc 0, le sommier etant le receptacle NDP 0 de la plateforme.

Une correction ne modifie jamais l'entree fautive : elle prend la forme
d'une nouvelle entree portant \code{corrige_id}. C'est le comportement du
classeur papier, ou la rature est interdite et la rectification se fait par
mention nouvelle.
}
\examples{
sommier_entree(
  foret_id = "3f2b1c4d-5e6f-4a7b-8c9d-0e1f2a3b4c5d",
  registre = 6L,
  date_evenement = "2026-04-15",
  auteur = "agent-01",
  payload = registre6_travaux(annee = 2026, nature_travaux = "degagement")
)

}
