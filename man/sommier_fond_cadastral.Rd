% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fond.R
\name{sommier_fond_cadastral}
\alias{sommier_fond_cadastral}
\title{Fond cadastral d'une commune}
\usage{
sommier_fond_cadastral(
  code_insee,
  couche = "parcelles",
  cache = NULL,
  force = FALSE
)
}
\arguments{
\item{code_insee}{Code INSEE de la commune, sur cinq caracteres.}

\item{couche}{L'une de \link{SOMMIER_COUCHES_CADASTRE}.}

\item{cache}{Repertoire de cache (defaut : le repertoire de cache de
l'utilisateur pour ce paquet).}

\item{force}{Retelecharger meme si le fichier est deja en cache.}
}
\value{
Invisiblement, un objet \code{sommier_fond} : \code{chemin}, \code{code_insee},
\code{couche}, \code{millesime}, \code{source}, \code{telecharge_le}.
}
\description{
Telecharge et met en cache une couche du cadastre pour une commune. Le
fichier obtenu sert de fond de plan aux cartes du sommier.
}
\details{
\strong{Le cadastre n'est pas une ecriture du sommier.} Rien de ce qui est
telecharge ici n'entre dans un registre, dans une empreinte ou dans un
manifeste : ce serait faire passer la donnee d'un tiers pour un constat du
gestionnaire, exactement ce que le registre existe pour empecher. Le fond
est un decor, date et source ; sa perte n'affecte rien.

\strong{Le telechargement est explicite.} Ni le rapport ni un export ne
declenchent d'appel reseau : un document de gestion doit pouvoir s'engendrer
sur un poste hors ligne. C'est l'appelant qui va chercher le fond, une fois,
et le passe ensuite a \code{\link[=sommier_rapport_quarto]{sommier_rapport_quarto()}}.

Le millesime est lu sur le serveur et conserve avec le fichier. Un fond sans
millesime induit en erreur des l'annee suivante - le parcellaire bouge.
}
\examples{
# Necessite un acces reseau :
# fond <- sommier_fond_cadastral("21200")

}
\seealso{
\code{\link[=sommier_fond_lire]{sommier_fond_lire()}}
}
