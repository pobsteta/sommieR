% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/horodatage.R
\name{tsa_lire_jeton}
\alias{tsa_lire_jeton}
\title{Lecture du contenu d'un jeton d'horodatage}
\usage{
tsa_lire_jeton(jeton)
}
\arguments{
\item{jeton}{Vecteur \code{raw} : le \code{TimeStampToken} rendu par
\code{\link[=tsa_horodater]{tsa_horodater()}}.}
}
\value{
Un objet \code{sommier_jeton_tsa} : \code{empreinte} (\code{raw}), \code{algorithme},
\code{date} (\code{POSIXct} UTC), \code{serie} et \code{nonce} (hexadecimal), \code{politique}
(OID pointe), \code{version}.
}
\description{
Rend le \code{TSTInfo} que le jeton encapsule (RFC 3161 section 2.4.2) : ce que
l'autorite a horodate, et quand.
}
\details{
Sans cette lecture, un jeton n'est qu'une colonne non vide. Le paquet
saurait dire qu'un visa « est horodate » sans savoir ni a quelle date, ni
sur quelle empreinte : un jeton parfaitement valide, obtenu pour une autre
tete de chaine - une autre foret, un autre exercice - passerait exactement
comme le bon.

Ce que cette fonction ne fait pas : verifier la signature de l'autorite et
la chaine de certification qui la rattache a une racine. Un \code{TSTInfo} lu
n'est pas un \code{TSTInfo} authentifie ; c'est l'objet du lot suivant, cadre
dans \code{specs/brief_probant-2}. La lecture n'exige, elle, aucun magasin de
confiance et aucun reseau.
}
\seealso{
\code{\link[=tsa_horodater]{tsa_horodater()}}, \code{\link[=tsa_lire_reponse]{tsa_lire_reponse()}}
}
