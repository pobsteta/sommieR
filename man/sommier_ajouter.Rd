% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ajouter.R
\name{sommier_ajouter}
\alias{sommier_ajouter}
\title{Ecriture d'entrees dans le sommier}
\usage{
sommier_ajouter(con, entrees)
}
\arguments{
\item{con}{Connexion DBI.}

\item{entrees}{Un objet \code{sommier_entree} ou une liste d'objets
\code{sommier_entree}, tous de la meme foret.}
}
\value{
Invisiblement, la liste des entrees chainees telles qu'inserees
(\code{seq}, \code{hash_prev} et \code{hash} renseignes).
}
\description{
Chaine puis insere une ou plusieurs entrees, en une transaction. La
position dans la chaine est determinee au moment de l'ecriture, a partir de
la tete courante lue en base : c'est le registre qui decide de la sequence,
pas l'appelant.
}
\details{
\strong{Serialisation des ecritures.} L'insertion prend un
\code{pg_advisory_xact_lock(hashtext(foret_id::text))}, conformement au brief
(section 6.3). Sans ce verrou, deux transactions concurrentes liraient la
meme tete, calculeraient le meme \code{hash_prev} et forkeraient la chaine. Le
verrou est pris \strong{avant} la lecture de la tete - le prendre apres ne
servirait a rien - et il est relache automatiquement a la fin de la
transaction.

\code{hashtext()} pouvant entrer en collision, deux forets differentes peuvent
partager un verrou : c'est sans consequence sur la correction (elles
s'attendent inutilement), et la contrainte \code{UNIQUE (foret_id, seq)} reste
le filet de securite si le verrou venait a manquer.
}
\seealso{
\code{\link[=sommier_entree]{sommier_entree()}}, \code{\link[=sommier_verifier]{sommier_verifier()}}
}
