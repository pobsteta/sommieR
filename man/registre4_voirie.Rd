% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registre4.R
\name{registre4_voirie}
\alias{registre4_voirie}
\title{Payload du registre 4 - voirie forestiere (imprimes A50D et A50D bis)}
\usage{
registre4_voirie(
  nom,
  revetement,
  longueur_m,
  largeur_chaussee_m = NULL,
  usage = NULL,
  ouverte_public = NULL,
  voirie_publique = FALSE,
  structure_chaussee = NULL,
  date_structure = NULL,
  observations = NULL
)
}
\arguments{
\item{nom}{Nom ou numero du troncon.}

\item{revetement}{L'un de \link{SOMMIER_REVETEMENTS}.}

\item{longueur_m}{Longueur en metres.}

\item{largeur_chaussee_m}{Largeur de chaussee (facultatif).}

\item{usage}{L'un de \link{SOMMIER_USAGES_VOIRIE} (facultatif).}

\item{ouverte_public}{Ouverture a la circulation publique (facultatif).}

\item{voirie_publique}{Le troncon releve de la voirie publique et non de la
voirie privee forestiere (defaut \code{FALSE}). L'imprime A50D les distingue,
et seule la voirie privee entre dans la densite.}

\item{structure_chaussee}{Description de la structure (facultatif).}

\item{date_structure}{Date de realisation de la structure (facultatif).}

\item{observations}{Observations libres (facultatif).}
}
\value{
Une liste nommee, prete a etre passee a \code{\link[=sommier_entree]{sommier_entree()}}.
}
\description{
Un troncon de voirie forestiere, avec son revetement, sa longueur, sa
largeur de chaussee et son usage.
}
\details{
L'imprime A50D inventorie la voirie et en tire des densites en km pour
100 hectares ; ces densites sont une vue calculee
(\code{\link[=sommier_densite_voirie]{sommier_densite_voirie()}}), pas une saisie. L'imprime A50D bis y ajoute,
route par route, l'usage et l'ouverture au public.

\code{ouverte_public} est distinct de l'usage : une route peut servir au
tourisme tout en etant fermee a la circulation motorisee, et c'est
precisement ce que l'imprime demande de tracer.
}
\examples{
registre4_voirie(
  nom = "Route du Haut-Bois", revetement = "empierree",
  longueur_m = 2400, largeur_chaussee_m = 4, usage = "exploitation",
  ouverte_public = FALSE
)

}
