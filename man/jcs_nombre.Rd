% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jcs.R
\name{jcs_nombre}
\alias{jcs_nombre}
\title{Serialisation d'un nombre selon ECMAScript \code{Number::toString}}
\usage{
jcs_nombre(m)
}
\arguments{
\item{m}{Nombre fini de longueur 1.}
}
\value{
Une chaine de caracteres.
}
\description{
RFC 8785 renvoie a l'algorithme ECMAScript : la representation decimale la
plus courte qui se relit exactement en IEEE 754 double precision, avec
bascule en notation exponentielle en dehors de la plage \code{1e-7} .. \code{1e21}.
}
\details{
Soient \code{n}, \code{k} et \code{s} les entiers tels que \code{k >= 1},
\verb{10^(k-1) <= s < 10^k}, \code{s * 10^(n-k) == m}, et \code{k} minimal. Alors :
\itemize{
\item \verb{k <= n <= 21} : chiffres de \code{s} suivis de \code{n - k} zeros ;
\item \verb{0 < n <= 21} : chiffres de \code{s} avec un point apres \code{n} chiffres ;
\item \verb{-6 < n <= 0} : \code{"0."}, \code{-n} zeros, puis les chiffres de \code{s} ;
\item sinon : notation exponentielle sur \code{n - 1}.
}

\code{k} minimal est obtenu en augmentant le nombre de chiffres significatifs
jusqu'a ce que la relecture redonne la valeur d'origine au bit pres.
}
\examples{
jcs_nombre(1)       # "1"
jcs_nombre(1e21)    # "1e+21"
jcs_nombre(1e-7)    # "1e-7"
jcs_nombre(0.1)     # "0.1"

}
