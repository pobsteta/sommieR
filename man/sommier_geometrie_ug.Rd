% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/carte.R
\name{sommier_geometrie_ug}
\alias{sommier_geometrie_ug}
\title{Contours des unites de gestion}
\usage{
sommier_geometrie_ug(con, foret_id, a_la_date = Sys.Date())
}
\arguments{
\item{con}{Connexion DBI.}

\item{foret_id}{UUID de la foret.}

\item{a_la_date}{Date de reference (defaut : aujourd'hui).}
}
\value{
Un \code{data.frame} : \code{uuid}, \code{numero_affichage}, \code{surface_ha}
(calculee par PostGIS sur le contour, \code{NA} faute de contour), \code{wkt}.
}
\description{
Rend, pour une foret, la geometrie en vigueur a une date de chaque unite de
gestion active, en WKT.
}
\details{
Seule la version de geometrie en vigueur a la date demandee est rendue :
\code{ug_geometrie} est versionnee, et le contour d'une unite change avec les
revisions d'amenagement. Une carte editee pour une periode passee doit
montrer le parcellaire de l'epoque, non celui d'aujourd'hui.

La geometrie sort en \strong{WKT et en Lambert-93}, les deux volontairement.
Le WKT parce que \code{sf::st_as_sfc()} a une methode caractere pour lui, la ou
lire une geometrie GeoJSON nue dependrait du pilote GDAL ; le Lambert-93
parce qu'une carte se mesure en metres, et que reprojeter ici deformerait
les surfaces que la colonne \code{surface_ha} annonce.

Une unite sans contour connu est \strong{rendue avec un WKT \code{NA}} plutot
qu'omise : c'est a l'appelant de decider s'il la signale ou l'ignore, et
l'escamoter ici lui retirerait ce choix.
}
\examples{
# Necessite une connexion :
# sommier_geometrie_ug(con, foret)

}
\seealso{
\code{\link[=sommier_indicateurs_ug]{sommier_indicateurs_ug()}}, \code{\link[=sommier_exporter_sig]{sommier_exporter_sig()}}
}
