% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quarto.R
\name{sommier_rapport_quarto}
\alias{sommier_rapport_quarto}
\title{Rapport de gestion anterieure en Quarto}
\usage{
sommier_rapport_quarto(
  con,
  foret_id,
  chemin,
  format = "html",
  debut = NULL,
  fin = NULL,
  referentiel = "psg",
  fond = NULL,
  quarto = Sys.which("quarto")
)
}
\arguments{
\item{con}{Connexion DBI.}

\item{foret_id}{UUID de la foret.}

\item{chemin}{Fichier de destination. Son extension doit s'accorder avec
\code{format}.}

\item{format}{\code{"html"} ou \code{"pdf"}.}

\item{debut, fin}{Bornes de la periode (voir \code{\link[=sommier_gestion_anterieure]{sommier_gestion_anterieure()}}).}

\item{referentiel}{L'un de \link{SOMMIER_REFERENTIELS}.}

\item{fond}{Fond cadastral a poser sous les cartes, tel que le rend
\code{\link[=sommier_fond_lire]{sommier_fond_lire()}} ; \code{NULL} pour s'en passer. Il se fournit et ne se
telecharge pas : le rendu ne doit declencher aucun appel reseau, sans
quoi un rapport cesserait d'etre editable hors ligne - et le meme rapport
rejoue plus tard changerait de fond sans le dire.}

\item{quarto}{Chemin de l'executable Quarto.}
}
\value{
Invisiblement, le chemin du document produit.
}
\description{
Rend la gestion anterieure sous forme de document Quarto — HTML autoportant
ou PDF — en y joignant l'etat de la chaine, la balance de possibilite, les
elements d'IBP et la desserte.
}
\details{
Les cartes sont portees par la meme extraction : les contours des unites de
gestion et leurs indicateurs voyagent en WKT dans le RDS, et le document les
convertit en \code{sf} au rendu. Le RDS n'exige donc pas \code{sf} pour etre relu.

Les donnees sont extraites de la base \strong{avant} le rendu et deposees dans un
fichier RDS que le document lit. Deux consequences voulues : aucun
identifiant de connexion ne circule dans le document ou ses parametres, et
le rendu est reproductible a l'identique sans acces a la base — on peut
rejouer un rapport des mois plus tard sur le meme instantane.

Le document porte l'empreinte de tete et l'etat de la chaine au moment de
l'edition. C'est une mise en forme, pas la preuve : la valeur probante reste
dans le registre, et \code{\link[=sommier_exporter_manifeste]{sommier_exporter_manifeste()}} est ce qui la transporte.
Le rapport le dit explicitement a son lecteur plutot que de laisser croire
qu'un PDF vaut attestation.

Quarto doit etre installe et joignable dans le \code{PATH} ; le rendu PDF exige
en outre une distribution LaTeX (\verb{quarto install tinytex} suffit).
}
\examples{
# Necessite une connexion et Quarto :
# sommier_rapport_quarto(con, foret, "gestion-anterieure.html")

}
\seealso{
\code{\link[=sommier_gestion_anterieure]{sommier_gestion_anterieure()}}, \code{\link[=sommier_rapport_markdown]{sommier_rapport_markdown()}}
}
